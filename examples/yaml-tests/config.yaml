# .revyl/config.yaml -- project configuration
#
# Generated by `revyl init`. Edit directly, then run `revyl config validate`.

project:
  id: 11111111-1111-4111-8111-111111111111

build:
  framework: android
  profiles:
    development:
      android:
        build_commands:
          - ./gradlew assembleDebug
        output_path: app/build/outputs/apk/debug/app-debug.apk
        timeout_seconds: 1800
