# Additional OpenAPI component schemas needed by handwritten CLI code but not
# reachable from an allowlisted operation. Prefer leaving this file empty;
# every exception must include a justification.

WorkflowCancelResponse
