# Runtime backend paths that intentionally do not contribute generated models.
# Keep this list narrow. Entries must still be present in production Go code;
# stale entries fail generation.

# Telemetry and SSE endpoints are deliberately hidden from backend OpenAPI.
/api/v1/monitor/stream/unified
/api/v1/telemetry/cli-analytics
/api/v1/telemetry/cli-spans
/api/v1/telemetry/cli-traces

# These handwritten compatibility endpoints are intentionally omitted from the
# generated CLI operation surface. They retain their shared response models and
# public wrappers; this list prevents duplicate generated transport methods.
/api/v1/scm/github/configs
/api/v1/scm/github/configs/push
/api/v1/workflows/status/cancel/%s

# YAML validation uses handwritten CLI transport models and is intentionally
# omitted from the customer-facing generated model surface.
/api/v1/tests/yaml/validate-yaml

# Proof-media publishing is a multipart upload, which generates no usable
# client, so its transport and response model are handwritten wrappers in
# internal/api.
/api/v1/reports-v3/sessions/%s/proof-media

# The proof-comment relay authenticates with a proof-run runtime credential
# rather than an ordinary API key, so generating it onto the customer-facing
# client surface would advertise an endpoint no ordinary caller can use.
/api/v1/scm/proof-runs/comment
